# Ignore the Kakadu source code (it's proprietary and can't be checked in)
src/main/docker/kakadu/*
kakadu/*

# Some build tools that are automatically generated
src/main/tools/eclipse/
src/main/tools/travis/

# A directory that's generated by the build process
src/main/generated

# Test image files which are downloaded at build time
src/test/resources/images/*.tif
src/test/resources/images/*.png
src/test/resources/images/*.jpg
src/test/resources/images/*.gif

# We ignore Maven's build directory
target/
